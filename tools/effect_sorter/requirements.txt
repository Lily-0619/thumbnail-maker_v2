# tools/effect_sorter 専用の追加依存（本体 requirements.txt は変更しない）
# D&D（ドラッグ&ドロップ）用。仕分けツールでは必須。
tkinterdnd2>=0.4.0
# Ollama（オラマ）翻訳用HTTPクライアント。本体 requirements.txt にも含まれるが単体導入用に明記。
requests>=2.31.0
# Pillow, customtkinter は本体 requirements.txt 済み
